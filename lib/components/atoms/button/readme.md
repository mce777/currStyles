Basic button:

    <div>
        <Button 
            type="button" 
            value="submit button"
            opStyle="primary" 
            handleClick={() => {alert("button was pressed")}} 
        />
    </div>
